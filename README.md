# davidheresy.github.io

## TODO

- [ ] Move site into `docs/`.
- [ ] Move CSS and JS into `/assets/`.
- [ ] Add bootstrap JS script for `<header>` and `<footer>`.
- [ ] Single bootstrap for `<meta>` and `<stylesheet>` as well?
    ```js
    document.querySelector('meta[name="description"]').setAttribute(
        "content", _desc
    );
    ```

